# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CIPlayground' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CIPlayground

  target 'CIPlaygroundTests' do
    inherit! :search_paths

    # Testing
  	pod 'Quick'
  	pod 'Nimble'

  end

  target 'CIPlaygroundUITests' do
    inherit! :search_paths

    # Testing
    pod 'Quick'
    pod 'Nimble'
    
  end

end
